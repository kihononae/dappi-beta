version: '3.8'

services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dappi-beta
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./src:/app/src
    command: ["node", "dist/index.js"]
